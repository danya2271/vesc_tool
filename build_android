#!/usr/bin/env bash
#
# Legacy build_android wrapper that delegates to build_android.sh
#

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [ $# -eq 0 ]; then
    exec "$SCRIPT_DIR/build_android.sh" --all --zip
else
    exec "$SCRIPT_DIR/build_android.sh" "$@"
fi


